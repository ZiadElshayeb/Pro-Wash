services:
  webhook:
    build: .
    container_name: pro-wash-webhook
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    networks:
      - n8n-network

networks:
  n8n-network:
    external: true
